export interface ITheme {
  blackColor: string;
  grayColor: string;
  whiteGreyColor: string;
}

const theme: ITheme = {
  blackColor: 'black',
  grayColor: 'gray',
  whiteGreyColor: '#F4F4F4'
};

export default theme;
